1) Создайте объект counter со свойством count и значением 0
Добавьте метод inc, который будет увеличивать значение свойства count на еденицу.
Результат в консоль
Метод запускайте через setInterval
Итог в консоле должно быть:
1
2
3
... и т.д.

2) Создайте объект time со свойством current и значением 0
Добавьте метод getTime, который будет записывать в свойство current текущую дату и отображать ее в консоль
Метод запускайте через setInterval
Итог в консоле должно быть:
12:00:01
12:00:02
12:00:03
...
Чтобы выводить дату в таком виде посмотрите еще раз про объект Date и как запрашивать у него часы, минуты и секунды

ДЗ ПО ТЕМЕ:
Создание калькуляторов через конструктор
На выходе должно быть
Создание калькулятора:

// суммирование
var sumsummation = Calculator("+", 10, 20);

// вычитание
var subtraction = Calculator("-", 10, 20)

// умножение
var multiplication = Calculator("*", 10, 20)

// деление
var division = new Calculator("/", 10, 20)

Сделать расчет

sumsummation.calc() // 30
subtraction.calc() // -10
multiplication.calc() // 200
division.calc() // 0.5


1) Создайте функцию конструктор Calculator, которая принимает 3 аргумента: операторо, число А, число Б.
Внутри конструктора есть метод calc, который должен производить вычисление с числами. Метод, как вы видите, вызывается у экземплятор (sumsummation, subtraction и т.д.) Пройдитесь по уроку "new Function".

ДЗ НЕ ПО ТЕМЕ(ДОПОЛЬНИТЕЛЬНОЕ):
1) Создайте функцию, которая принимает в качестве аргумента слово. Функция должна возвращать текст забором

helloFrom2007("Верните мой дветысячи седьмой") // ВеРнИтЕ МоЙ ДвЕтЫсЯчИ СеДьМоЙ

Возможно вам для этой задачи понадобится нахождение четных и нечетных чисел. Для этого воспользуйтиесь оператором, который возвращает остаток от деления.

2) Создайте элемент div. С шириной 100px, высотой 100px и рамкой. Через функцию setInterval сделайте так чтобы через каждый интервал времени у элемента div постепенно добавлялось значение в свойство border-radius. Элемент из квадрата должен превратится в круг.

3) Создать функцию по складыванию чисел. от 1 до 100. 1+2+3 и т.д. Результат должен быть 5050

4) Создать функцию по складыванию как чисел от 1 до 1 000 000. По следующей схеме. 123 как 1+2+3. 1+2+3 + 1+2+4 + 1+2+5. Результат должне быть 27 000 001